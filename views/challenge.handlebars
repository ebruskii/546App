<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>
<body>
    <div class="container mt-4">
        <div class="card">
            <div class="card-body">
                <h2 class="card-title">{{challenge.title}}</h2>
                <h5 class="card-subtitle mb-2 text-muted">{{challenge.description}}</h5>
                <ul class="list-group list-group-flush">
                    <li class="list-group-item">Type of Workout: {{challenge.type}}</li>
                    <li class="list-group-item">Challenge: {{challenge.amountOfWorkout}} {{challenge.unitOfWorkout}}</li>
                    <li class="list-group-item">Start Date: {{challenge.startDate}}</li>
                    <li class="list-group-item">End Date: {{ challenge.endDate}}</li>
                </ul>
            </div>
        </div>

        <h3 class="mt-4">Top 10 Leaderboard</h3>
        <ul class="list-group mb-4">
            {{#each leaderboard}}
            <li class="list-group-item"><a href="/profile/{{this.id}}" class="list-group-item-action" > {{this.name}}: {{this.value}} {{challenge.unitOfWorkout}}</a></li>
            {{/each}}
        </ul>

        <h3 class="mt-4">Your Current Progress</h3>
        <ul class="list-group mb-4">
            {{#if burn}}
            <li class="list-group-item" >You have burned {{currentUserLeaderboard}} calories.</li>
            {{else}}
             <li class="list-group-item" >You have completed {{currentUserLeaderboard}} {{challenge.unitOfWorkout}}.</li>
            {{/if}}
        </ul>
        
        {{!-- {{#if workouts.length}}
        <div class="list-group">
            {{#each workouts}}
            <a href="#" class="list-group-item list-group-item-action flex-column align-items-start">
                <div class="d-flex w-100 justify-content-between">
                    <h5 class="mb-1">{{this.type}}</h5>
                    <small>{{this.duration}} mins</small>
                </div>
                <p class="mb-1">{{this.description}}</p>
                <small>Workout on: {{ this.date}}</small>
            </a>
            {{/each}}
        </div>
        {{else}}
        <div class="alert alert-info">No workouts recorded.</div>
        {{/if}} --}}

        {{!-- <button class="btn btn-primary mb-4" id="makeFriendButton">Make a Friend</button> --}}
    </div>
    <div id="who" style="display:none">{{who}}</div>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.7.12/umd.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    {{!-- <script>
        document.getElementById('makeFriendButton').addEventListener('click', function() {
            let who = document.getElementById('who').textContent;
            fetch(`/profile/addFriend/${who}`, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                    // Add any necessary CSRF tokens or authentication headers
                }
            })
            .then(response => response.json())
            .then(data => {
                alert('Friend request sent!');
                // Optionally update the UI to reflect the new friend status
            })
            .catch(error => console.error('Error sending friend request:', error));
            location.reload();
        });
    </script> --}}
</body>
</html>
